#include<stdio.h>
int main()
{
    int n,q;
    scanf("%d %d", &n,&q);
    int arr[n];
    int search[q];
    int result[n];
    for (int i = 0; i < n; i++)
    {
        scanf("%d",&arr[i]);
    }
    for (int i = 0; i < q; i++)
    {
        scanf("%d", &search[q]);
    }
    for (int i = 0; i < n ; i++)
    {
        for (int j = i; j < n; i++)
        {
            /* code */
        }
        
    }
    
    
    
    return 0;
}